# CLI (ghost_check.py). scikit-learn is pinned: the model file is a pickle
# that must be loaded with the version it was trained with.
scikit-learn==1.9.1
numpy>=2.0
scipy>=1.11
joblib>=1.3
pypdf>=4.0
